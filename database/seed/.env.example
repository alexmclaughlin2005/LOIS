# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Note: Use the service role key (not anon key) for seed scripts
# This key has full database access and should never be exposed in client code
